# ByteGDPS Demonlist

лист демонов нашего сервака прямо в игре. залетел на уровень — видишь `#7 ByteGDPS` и сразу понятно, насколько всё плохо.

шо внутри:
- бейдж с местом в топе на каждом демоне из списка
- отдельный экран: поиск, страницы, рандом
- находит даже наши копии — id не сошёлся, найдёт по названию

данные: bytegdps.ru/api/demonlist.php (тот же лист что на сайте), обновляется раз в час.

сборка: пушнул — CI сам собрал винду + оба андроида. готовый .geode в Actions.

---
движок — форк IntegratedDemonlist by hiimjasmine00 (MIT), ему респект. лист наш.
